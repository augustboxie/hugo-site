---
label: post
hide_body: true
fields:
- name: img
  type: file
  config:
    maxSize: 250
  label: IMG
  description: image
- name: title
  type: text
  config:
    required: true
  label: title
  description: title-tag
  default: title
